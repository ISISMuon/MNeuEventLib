# API Reference

The API reference is stored in this folder when the docs are built.

From the root directory of the project, build the API reference with:
```
cargo doc --document-private-items --no-deps --target-dir "./docs/source/_static/api/"
```
